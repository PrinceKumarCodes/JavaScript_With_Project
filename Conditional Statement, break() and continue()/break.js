// break exits the loop immediately,
// while continue skips the current iteration and moves to the next.
for (let i = 1; i <= 10; i++) {
  if (i == 5) {
    break;
  } else {
    console.log(i + " ");
  }
}
