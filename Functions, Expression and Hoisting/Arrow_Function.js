const getExp = (x, y) => {
  // => is called arrow function
  let ans = x ** y;
  return ans;
};
const output = getExp(2, 2);
console.log(output);
// ---------------Built-in Functions-------------
// JavaScript comes with several built-in functions, such as setTimeout, setInterval, and addEventListener, which take functions as arguments.
